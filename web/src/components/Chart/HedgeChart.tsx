export function HedgeChart() {
  // TODO Phase 2: render Lightweight Charts with historical + live candles + tick price line
  // TODO Phase 2: setup lines from store.chartLines
  // TODO Phase 2: ChartContextMenu for right-click set Entry/SL/TP
  // TODO Phase 3: order overlay (open + pending orders)
  // TODO Phase 3: drag SL/TP for open orders
  return (
    <div className="h-full bg-white border border-gray-200 rounded p-4 flex items-center justify-center">
      <div className="text-center text-gray-400">
        <p className="text-sm font-medium">HedgeChart</p>
        <p className="text-xs mt-1">Phase 2: live candles + chart interactions</p>
      </div>
    </div>
  )
}
