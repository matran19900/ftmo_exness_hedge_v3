export function HistoryTab() {
  // TODO Phase 3: GET /api/positions/history + WS update on hedge_closed
  return (
    <div className="p-6 text-center text-gray-400">
      <p className="text-sm font-medium">No closed positions yet</p>
      <p className="text-xs mt-1">Phase 3: closed orders archive</p>
    </div>
  )
}
