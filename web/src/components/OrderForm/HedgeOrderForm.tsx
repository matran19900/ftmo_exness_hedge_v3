export function HedgeOrderForm() {
  // TODO Phase 2: PairPicker + symbol picker + order type tabs (Market/Limit/Stop)
  // TODO Phase 2: Entry/SL/TP/Risk inputs + VolumeCalculator + ConversionRateInfo
  // TODO Phase 3: submit handler → POST /api/orders
  return (
    <div className="h-full bg-white border border-gray-200 rounded p-4 overflow-y-auto">
      <div className="text-center text-gray-400">
        <p className="text-sm font-medium">HedgeOrderForm</p>
        <p className="text-xs mt-1">Phase 2: pair picker + order inputs + volume calc</p>
      </div>
    </div>
  )
}
